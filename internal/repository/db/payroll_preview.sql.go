// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: payroll_preview.sql

package db

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const listNationalHolidaysInRange = `-- name: ListNationalHolidaysInRange :many
SELECT
    holiday_date,
    name
FROM national_holidays
WHERE country_code = $1
  AND is_national = TRUE
  AND holiday_date >= $2
  AND holiday_date <= $3
ORDER BY holiday_date ASC
`

type ListNationalHolidaysInRangeParams struct {
	CountryCode string      `json:"country_code"`
	StartDate   pgtype.Date `json:"start_date"`
	EndDate     pgtype.Date `json:"end_date"`
}

type ListNationalHolidaysInRangeRow struct {
	HolidayDate pgtype.Date `json:"holiday_date"`
	Name        string      `json:"name"`
}

func (q *Queries) ListNationalHolidaysInRange(ctx context.Context, arg ListNationalHolidaysInRangeParams) ([]ListNationalHolidaysInRangeRow, error) {
	rows, err := q.db.Query(ctx, listNationalHolidaysInRange, arg.CountryCode, arg.StartDate, arg.EndDate)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListNationalHolidaysInRangeRow{}
	for rows.Next() {
		var i ListNationalHolidaysInRangeRow
		if err := rows.Scan(&i.HolidayDate, &i.Name); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listPayrollPreviewTimeEntries = `-- name: ListPayrollPreviewTimeEntries :many
SELECT
    te.id,
    te.employee_id,
    ep.first_name AS employee_first_name,
    ep.last_name AS employee_last_name,
    te.entry_date,
    te.start_time,
    te.end_time,
    te.break_minutes,
    te.hour_type,
    COALESCE(cc.contract_type, ep.contract_type) AS contract_type,
    COALESCE(cc.contract_rate, ep.contract_rate) AS contract_rate,
    COALESCE(cc.irregular_hours_profile, ep.irregular_hours_profile) AS irregular_hours_profile
FROM time_entries te
JOIN employee_profile ep ON ep.id = te.employee_id
LEFT JOIN LATERAL (
    SELECT
        c.contract_type,
        c.contract_rate,
        c.irregular_hours_profile
    FROM employee_contract_changes c
    WHERE c.employee_id = te.employee_id
      AND c.effective_from <= te.entry_date
    ORDER BY c.effective_from DESC, c.created_at DESC
    LIMIT 1
) cc ON TRUE
WHERE te.employee_id = $1
  AND te.status = 'approved'::time_entry_status_enum
  AND te.paid_period_id IS NULL
  AND te.hour_type IN (
      'normal'::time_entry_hour_type_enum,
      'overtime'::time_entry_hour_type_enum,
      'travel'::time_entry_hour_type_enum,
      'training'::time_entry_hour_type_enum
  )
  AND te.entry_date >= $2
  AND te.entry_date <= $3
ORDER BY te.entry_date ASC, te.start_time ASC, te.created_at ASC
`

type ListPayrollPreviewTimeEntriesParams struct {
	EmployeeID  uuid.UUID   `json:"employee_id"`
	PeriodStart pgtype.Date `json:"period_start"`
	PeriodEnd   pgtype.Date `json:"period_end"`
}

type ListPayrollPreviewTimeEntriesRow struct {
	ID                    uuid.UUID                 `json:"id"`
	EmployeeID            uuid.UUID                 `json:"employee_id"`
	EmployeeFirstName     string                    `json:"employee_first_name"`
	EmployeeLastName      string                    `json:"employee_last_name"`
	EntryDate             pgtype.Date               `json:"entry_date"`
	StartTime             pgtype.Time               `json:"start_time"`
	EndTime               pgtype.Time               `json:"end_time"`
	BreakMinutes          int32                     `json:"break_minutes"`
	HourType              TimeEntryHourTypeEnum     `json:"hour_type"`
	ContractType          EmployeeContractTypeEnum  `json:"contract_type"`
	ContractRate          *float64                  `json:"contract_rate"`
	IrregularHoursProfile IrregularHoursProfileEnum `json:"irregular_hours_profile"`
}

func (q *Queries) ListPayrollPreviewTimeEntries(ctx context.Context, arg ListPayrollPreviewTimeEntriesParams) ([]ListPayrollPreviewTimeEntriesRow, error) {
	rows, err := q.db.Query(ctx, listPayrollPreviewTimeEntries, arg.EmployeeID, arg.PeriodStart, arg.PeriodEnd)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListPayrollPreviewTimeEntriesRow{}
	for rows.Next() {
		var i ListPayrollPreviewTimeEntriesRow
		if err := rows.Scan(
			&i.ID,
			&i.EmployeeID,
			&i.EmployeeFirstName,
			&i.EmployeeLastName,
			&i.EntryDate,
			&i.StartTime,
			&i.EndTime,
			&i.BreakMinutes,
			&i.HourType,
			&i.ContractType,
			&i.ContractRate,
			&i.IrregularHoursProfile,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
