// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: salary_scale_steps.sql

package db

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const listSalaryScaleSteps = `-- name: ListSalaryScaleSteps :many
SELECT
    css.id,
    css.salary_table_id,
    css.scale,
    css.step,
    css.ip_number,
    css.monthly_salary,
    css.hourly_rate,
    cst.cao_code,
    cst.name AS salary_table_name,
    cst.effective_from,
    cst.effective_to
FROM cao_salary_scale_steps css
JOIN cao_salary_tables cst ON cst.id = css.salary_table_id
WHERE (CASE WHEN $1::bool THEN cst.effective_from <= CURRENT_DATE AND (cst.effective_to IS NULL OR cst.effective_to >= CURRENT_DATE) ELSE TRUE END)
ORDER BY cst.effective_from DESC, css.scale ASC, css.step ASC
`

type ListSalaryScaleStepsRow struct {
	ID              uuid.UUID   `json:"id"`
	SalaryTableID   uuid.UUID   `json:"salary_table_id"`
	Scale           int32       `json:"scale"`
	Step            string      `json:"step"`
	IpNumber        *int32      `json:"ip_number"`
	MonthlySalary   float64     `json:"monthly_salary"`
	HourlyRate      float64     `json:"hourly_rate"`
	CaoCode         string      `json:"cao_code"`
	SalaryTableName string      `json:"salary_table_name"`
	EffectiveFrom   pgtype.Date `json:"effective_from"`
	EffectiveTo     pgtype.Date `json:"effective_to"`
}

func (q *Queries) ListSalaryScaleSteps(ctx context.Context, activeOnly *bool) ([]ListSalaryScaleStepsRow, error) {
	rows, err := q.db.Query(ctx, listSalaryScaleSteps, activeOnly)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListSalaryScaleStepsRow{}
	for rows.Next() {
		var i ListSalaryScaleStepsRow
		if err := rows.Scan(
			&i.ID,
			&i.SalaryTableID,
			&i.Scale,
			&i.Step,
			&i.IpNumber,
			&i.MonthlySalary,
			&i.HourlyRate,
			&i.CaoCode,
			&i.SalaryTableName,
			&i.EffectiveFrom,
			&i.EffectiveTo,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
