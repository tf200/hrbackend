// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: overtime.sql

package db

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const approveOvertimeEntry = `-- name: ApproveOvertimeEntry :one
WITH updated_overtime_entry AS (
    UPDATE overtime_entries
    SET
        status = 'approved'::overtime_status_enum,
        approved_at = NOW(),
        approved_by_employee_id = $1,
        rejection_reason = NULL,
        updated_at = NOW()
    WHERE overtime_entries.id = $2
    RETURNING
        id,
        employee_id,
        schedule_id,
        entry_date,
        minutes,
        reason,
        description,
        status,
        submitted_at,
        approved_at,
        approved_by_employee_id,
        rejection_reason,
        paid_period_id,
        created_at,
        updated_at
)
SELECT
    oe.id,
    oe.employee_id,
    oe.schedule_id,
    oe.entry_date,
    oe.minutes,
    oe.reason,
    oe.description,
    oe.status,
    oe.submitted_at,
    oe.approved_at,
    oe.approved_by_employee_id,
    oe.rejection_reason,
    oe.paid_period_id,
    oe.created_at,
    oe.updated_at,
    ep.first_name AS employee_first_name,
    ep.last_name AS employee_last_name,
    ap.first_name AS approved_by_first_name,
    ap.last_name AS approved_by_last_name
FROM updated_overtime_entry oe
JOIN employee_profile ep ON ep.id = oe.employee_id
LEFT JOIN employee_profile ap ON ap.id = oe.approved_by_employee_id
`

type ApproveOvertimeEntryParams struct {
	ApprovedByEmployeeID *uuid.UUID `json:"approved_by_employee_id"`
	ID                   uuid.UUID  `json:"id"`
}

type ApproveOvertimeEntryRow struct {
	ID                   uuid.UUID          `json:"id"`
	EmployeeID           uuid.UUID          `json:"employee_id"`
	ScheduleID           *uuid.UUID         `json:"schedule_id"`
	EntryDate            pgtype.Date        `json:"entry_date"`
	Minutes              int32              `json:"minutes"`
	Reason               OvertimeReasonEnum `json:"reason"`
	Description          *string            `json:"description"`
	Status               OvertimeStatusEnum `json:"status"`
	SubmittedAt          pgtype.Timestamptz `json:"submitted_at"`
	ApprovedAt           pgtype.Timestamptz `json:"approved_at"`
	ApprovedByEmployeeID *uuid.UUID         `json:"approved_by_employee_id"`
	RejectionReason      *string            `json:"rejection_reason"`
	PaidPeriodID         *uuid.UUID         `json:"paid_period_id"`
	CreatedAt            pgtype.Timestamptz `json:"created_at"`
	UpdatedAt            pgtype.Timestamptz `json:"updated_at"`
	EmployeeFirstName    string             `json:"employee_first_name"`
	EmployeeLastName     string             `json:"employee_last_name"`
	ApprovedByFirstName  *string            `json:"approved_by_first_name"`
	ApprovedByLastName   *string            `json:"approved_by_last_name"`
}

func (q *Queries) ApproveOvertimeEntry(ctx context.Context, arg ApproveOvertimeEntryParams) (ApproveOvertimeEntryRow, error) {
	row := q.db.QueryRow(ctx, approveOvertimeEntry, arg.ApprovedByEmployeeID, arg.ID)
	var i ApproveOvertimeEntryRow
	err := row.Scan(
		&i.ID,
		&i.EmployeeID,
		&i.ScheduleID,
		&i.EntryDate,
		&i.Minutes,
		&i.Reason,
		&i.Description,
		&i.Status,
		&i.SubmittedAt,
		&i.ApprovedAt,
		&i.ApprovedByEmployeeID,
		&i.RejectionReason,
		&i.PaidPeriodID,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.EmployeeFirstName,
		&i.EmployeeLastName,
		&i.ApprovedByFirstName,
		&i.ApprovedByLastName,
	)
	return i, err
}

const createOvertimeEntry = `-- name: CreateOvertimeEntry :one
WITH inserted_overtime_entry AS (
    INSERT INTO overtime_entries (
        employee_id,
        schedule_id,
        entry_date,
        minutes,
        reason,
        description
    ) VALUES (
        $1,
        $2,
        $3,
        $4,
        $5,
        $6
    )
    RETURNING
        id,
        employee_id,
        schedule_id,
        entry_date,
        minutes,
        reason,
        description,
        status,
        submitted_at,
        approved_at,
        approved_by_employee_id,
        rejection_reason,
        paid_period_id,
        created_at,
        updated_at
)
SELECT
    oe.id,
    oe.employee_id,
    oe.schedule_id,
    oe.entry_date,
    oe.minutes,
    oe.reason,
    oe.description,
    oe.status,
    oe.submitted_at,
    oe.approved_at,
    oe.approved_by_employee_id,
    oe.rejection_reason,
    oe.paid_period_id,
    oe.created_at,
    oe.updated_at,
    ep.first_name AS employee_first_name,
    ep.last_name AS employee_last_name,
    ap.first_name AS approved_by_first_name,
    ap.last_name AS approved_by_last_name
FROM inserted_overtime_entry oe
JOIN employee_profile ep ON ep.id = oe.employee_id
LEFT JOIN employee_profile ap ON ap.id = oe.approved_by_employee_id
`

type CreateOvertimeEntryParams struct {
	EmployeeID  uuid.UUID          `json:"employee_id"`
	ScheduleID  *uuid.UUID         `json:"schedule_id"`
	EntryDate   pgtype.Date        `json:"entry_date"`
	Minutes     int32              `json:"minutes"`
	Reason      OvertimeReasonEnum `json:"reason"`
	Description *string            `json:"description"`
}

type CreateOvertimeEntryRow struct {
	ID                   uuid.UUID          `json:"id"`
	EmployeeID           uuid.UUID          `json:"employee_id"`
	ScheduleID           *uuid.UUID         `json:"schedule_id"`
	EntryDate            pgtype.Date        `json:"entry_date"`
	Minutes              int32              `json:"minutes"`
	Reason               OvertimeReasonEnum `json:"reason"`
	Description          *string            `json:"description"`
	Status               OvertimeStatusEnum `json:"status"`
	SubmittedAt          pgtype.Timestamptz `json:"submitted_at"`
	ApprovedAt           pgtype.Timestamptz `json:"approved_at"`
	ApprovedByEmployeeID *uuid.UUID         `json:"approved_by_employee_id"`
	RejectionReason      *string            `json:"rejection_reason"`
	PaidPeriodID         *uuid.UUID         `json:"paid_period_id"`
	CreatedAt            pgtype.Timestamptz `json:"created_at"`
	UpdatedAt            pgtype.Timestamptz `json:"updated_at"`
	EmployeeFirstName    string             `json:"employee_first_name"`
	EmployeeLastName     string             `json:"employee_last_name"`
	ApprovedByFirstName  *string            `json:"approved_by_first_name"`
	ApprovedByLastName   *string            `json:"approved_by_last_name"`
}

func (q *Queries) CreateOvertimeEntry(ctx context.Context, arg CreateOvertimeEntryParams) (CreateOvertimeEntryRow, error) {
	row := q.db.QueryRow(ctx, createOvertimeEntry,
		arg.EmployeeID,
		arg.ScheduleID,
		arg.EntryDate,
		arg.Minutes,
		arg.Reason,
		arg.Description,
	)
	var i CreateOvertimeEntryRow
	err := row.Scan(
		&i.ID,
		&i.EmployeeID,
		&i.ScheduleID,
		&i.EntryDate,
		&i.Minutes,
		&i.Reason,
		&i.Description,
		&i.Status,
		&i.SubmittedAt,
		&i.ApprovedAt,
		&i.ApprovedByEmployeeID,
		&i.RejectionReason,
		&i.PaidPeriodID,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.EmployeeFirstName,
		&i.EmployeeLastName,
		&i.ApprovedByFirstName,
		&i.ApprovedByLastName,
	)
	return i, err
}

const deleteOvertimeEntry = `-- name: DeleteOvertimeEntry :one
DELETE FROM overtime_entries
WHERE id = $1
RETURNING id
`

func (q *Queries) DeleteOvertimeEntry(ctx context.Context, id uuid.UUID) (uuid.UUID, error) {
	row := q.db.QueryRow(ctx, deleteOvertimeEntry, id)
	var id_2 uuid.UUID
	err := row.Scan(&id_2)
	return id_2, err
}

const getCurrentMonthOvertimeStats = `-- name: GetCurrentMonthOvertimeStats :one
WITH ranges AS (
    SELECT
        date_trunc('month', CURRENT_DATE)::date AS month_start,
        (date_trunc('month', CURRENT_DATE) + INTERVAL '1 month')::date AS next_month_start
)
SELECT
    COALESCE(SUM(oe.minutes) FILTER (
        WHERE oe.status = 'approved'::overtime_status_enum
    ), 0)::BIGINT AS total_approved_minutes,
    COALESCE(COUNT(*) FILTER (
        WHERE oe.status = 'submitted'::overtime_status_enum
    ), 0)::BIGINT AS total_awaiting_approval,
    COALESCE(COUNT(*) FILTER (
        WHERE oe.status = 'approved'::overtime_status_enum
    ), 0)::BIGINT AS total_approved,
    COALESCE(COUNT(*) FILTER (
        WHERE oe.status = 'submitted'::overtime_status_enum
    ), 0)::BIGINT AS total_submitted
FROM ranges r
CROSS JOIN overtime_entries oe
WHERE oe.entry_date >= r.month_start
  AND oe.entry_date < r.next_month_start
`

type GetCurrentMonthOvertimeStatsRow struct {
	TotalApprovedMinutes  int64 `json:"total_approved_minutes"`
	TotalAwaitingApproval int64 `json:"total_awaiting_approval"`
	TotalApproved         int64 `json:"total_approved"`
	TotalSubmitted        int64 `json:"total_submitted"`
}

func (q *Queries) GetCurrentMonthOvertimeStats(ctx context.Context) (GetCurrentMonthOvertimeStatsRow, error) {
	row := q.db.QueryRow(ctx, getCurrentMonthOvertimeStats)
	var i GetCurrentMonthOvertimeStatsRow
	err := row.Scan(
		&i.TotalApprovedMinutes,
		&i.TotalAwaitingApproval,
		&i.TotalApproved,
		&i.TotalSubmitted,
	)
	return i, err
}

const getMyCurrentMonthOvertimeStats = `-- name: GetMyCurrentMonthOvertimeStats :one
WITH ranges AS (
    SELECT
        date_trunc('month', CURRENT_DATE)::date AS month_start,
        (date_trunc('month', CURRENT_DATE) + INTERVAL '1 month')::date AS next_month_start
)
SELECT
    COALESCE(SUM(oe.minutes) FILTER (
        WHERE oe.status = 'approved'::overtime_status_enum
    ), 0)::BIGINT AS total_approved_minutes,
    COALESCE(COUNT(*) FILTER (
        WHERE oe.status = 'submitted'::overtime_status_enum
    ), 0)::BIGINT AS total_awaiting_approval,
    COALESCE(COUNT(*) FILTER (
        WHERE oe.status = 'approved'::overtime_status_enum
    ), 0)::BIGINT AS total_approved,
    COALESCE(COUNT(*) FILTER (
        WHERE oe.status = 'submitted'::overtime_status_enum
    ), 0)::BIGINT AS total_submitted
FROM ranges r
CROSS JOIN overtime_entries oe
WHERE oe.employee_id = $1
  AND oe.entry_date >= r.month_start
  AND oe.entry_date < r.next_month_start
`

type GetMyCurrentMonthOvertimeStatsRow struct {
	TotalApprovedMinutes  int64 `json:"total_approved_minutes"`
	TotalAwaitingApproval int64 `json:"total_awaiting_approval"`
	TotalApproved         int64 `json:"total_approved"`
	TotalSubmitted        int64 `json:"total_submitted"`
}

func (q *Queries) GetMyCurrentMonthOvertimeStats(ctx context.Context, employeeID uuid.UUID) (GetMyCurrentMonthOvertimeStatsRow, error) {
	row := q.db.QueryRow(ctx, getMyCurrentMonthOvertimeStats, employeeID)
	var i GetMyCurrentMonthOvertimeStatsRow
	err := row.Scan(
		&i.TotalApprovedMinutes,
		&i.TotalAwaitingApproval,
		&i.TotalApproved,
		&i.TotalSubmitted,
	)
	return i, err
}

const getOvertimeEntryByID = `-- name: GetOvertimeEntryByID :one
SELECT
    oe.id,
    oe.employee_id,
    oe.schedule_id,
    oe.entry_date,
    oe.minutes,
    oe.reason,
    oe.description,
    oe.status,
    oe.submitted_at,
    oe.approved_at,
    oe.approved_by_employee_id,
    oe.rejection_reason,
    oe.paid_period_id,
    oe.created_at,
    oe.updated_at,
    ep.first_name AS employee_first_name,
    ep.last_name AS employee_last_name,
    ap.first_name AS approved_by_first_name,
    ap.last_name AS approved_by_last_name
FROM overtime_entries oe
JOIN employee_profile ep ON ep.id = oe.employee_id
LEFT JOIN employee_profile ap ON ap.id = oe.approved_by_employee_id
WHERE oe.id = $1
`

type GetOvertimeEntryByIDRow struct {
	ID                   uuid.UUID          `json:"id"`
	EmployeeID           uuid.UUID          `json:"employee_id"`
	ScheduleID           *uuid.UUID         `json:"schedule_id"`
	EntryDate            pgtype.Date        `json:"entry_date"`
	Minutes              int32              `json:"minutes"`
	Reason               OvertimeReasonEnum `json:"reason"`
	Description          *string            `json:"description"`
	Status               OvertimeStatusEnum `json:"status"`
	SubmittedAt          pgtype.Timestamptz `json:"submitted_at"`
	ApprovedAt           pgtype.Timestamptz `json:"approved_at"`
	ApprovedByEmployeeID *uuid.UUID         `json:"approved_by_employee_id"`
	RejectionReason      *string            `json:"rejection_reason"`
	PaidPeriodID         *uuid.UUID         `json:"paid_period_id"`
	CreatedAt            pgtype.Timestamptz `json:"created_at"`
	UpdatedAt            pgtype.Timestamptz `json:"updated_at"`
	EmployeeFirstName    string             `json:"employee_first_name"`
	EmployeeLastName     string             `json:"employee_last_name"`
	ApprovedByFirstName  *string            `json:"approved_by_first_name"`
	ApprovedByLastName   *string            `json:"approved_by_last_name"`
}

func (q *Queries) GetOvertimeEntryByID(ctx context.Context, id uuid.UUID) (GetOvertimeEntryByIDRow, error) {
	row := q.db.QueryRow(ctx, getOvertimeEntryByID, id)
	var i GetOvertimeEntryByIDRow
	err := row.Scan(
		&i.ID,
		&i.EmployeeID,
		&i.ScheduleID,
		&i.EntryDate,
		&i.Minutes,
		&i.Reason,
		&i.Description,
		&i.Status,
		&i.SubmittedAt,
		&i.ApprovedAt,
		&i.ApprovedByEmployeeID,
		&i.RejectionReason,
		&i.PaidPeriodID,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.EmployeeFirstName,
		&i.EmployeeLastName,
		&i.ApprovedByFirstName,
		&i.ApprovedByLastName,
	)
	return i, err
}

const listMyOvertimeEntriesPaginated = `-- name: ListMyOvertimeEntriesPaginated :many
WITH filtered AS (
    SELECT
        oe.id,
        oe.employee_id,
        oe.schedule_id,
        oe.entry_date,
        oe.minutes,
        oe.reason,
        oe.description,
        oe.status,
        oe.submitted_at,
        oe.approved_at,
        oe.approved_by_employee_id,
        oe.rejection_reason,
        oe.paid_period_id,
        oe.created_at,
        oe.updated_at,
        ep.first_name AS employee_first_name,
        ep.last_name AS employee_last_name,
        ap.first_name AS approved_by_first_name,
        ap.last_name AS approved_by_last_name,
        COUNT(*) OVER() AS total_count
    FROM overtime_entries oe
    JOIN employee_profile ep ON ep.id = oe.employee_id
    LEFT JOIN employee_profile ap ON ap.id = oe.approved_by_employee_id
    WHERE oe.employee_id = $3
      AND (
        $4::overtime_status_enum IS NULL
        OR oe.status = $4::overtime_status_enum
    )
    ORDER BY oe.created_at DESC
)
SELECT id, employee_id, schedule_id, entry_date, minutes, reason, description, status, submitted_at, approved_at, approved_by_employee_id, rejection_reason, paid_period_id, created_at, updated_at, employee_first_name, employee_last_name, approved_by_first_name, approved_by_last_name, total_count
FROM filtered
LIMIT $2
OFFSET $1
`

type ListMyOvertimeEntriesPaginatedParams struct {
	OffsetCount int32               `json:"offset_count"`
	LimitCount  int32               `json:"limit_count"`
	EmployeeID  uuid.UUID           `json:"employee_id"`
	Status      *OvertimeStatusEnum `json:"status"`
}

type ListMyOvertimeEntriesPaginatedRow struct {
	ID                   uuid.UUID          `json:"id"`
	EmployeeID           uuid.UUID          `json:"employee_id"`
	ScheduleID           *uuid.UUID         `json:"schedule_id"`
	EntryDate            pgtype.Date        `json:"entry_date"`
	Minutes              int32              `json:"minutes"`
	Reason               OvertimeReasonEnum `json:"reason"`
	Description          *string            `json:"description"`
	Status               OvertimeStatusEnum `json:"status"`
	SubmittedAt          pgtype.Timestamptz `json:"submitted_at"`
	ApprovedAt           pgtype.Timestamptz `json:"approved_at"`
	ApprovedByEmployeeID *uuid.UUID         `json:"approved_by_employee_id"`
	RejectionReason      *string            `json:"rejection_reason"`
	PaidPeriodID         *uuid.UUID         `json:"paid_period_id"`
	CreatedAt            pgtype.Timestamptz `json:"created_at"`
	UpdatedAt            pgtype.Timestamptz `json:"updated_at"`
	EmployeeFirstName    string             `json:"employee_first_name"`
	EmployeeLastName     string             `json:"employee_last_name"`
	ApprovedByFirstName  *string            `json:"approved_by_first_name"`
	ApprovedByLastName   *string            `json:"approved_by_last_name"`
	TotalCount           int64              `json:"total_count"`
}

func (q *Queries) ListMyOvertimeEntriesPaginated(ctx context.Context, arg ListMyOvertimeEntriesPaginatedParams) ([]ListMyOvertimeEntriesPaginatedRow, error) {
	rows, err := q.db.Query(ctx, listMyOvertimeEntriesPaginated,
		arg.OffsetCount,
		arg.LimitCount,
		arg.EmployeeID,
		arg.Status,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListMyOvertimeEntriesPaginatedRow{}
	for rows.Next() {
		var i ListMyOvertimeEntriesPaginatedRow
		if err := rows.Scan(
			&i.ID,
			&i.EmployeeID,
			&i.ScheduleID,
			&i.EntryDate,
			&i.Minutes,
			&i.Reason,
			&i.Description,
			&i.Status,
			&i.SubmittedAt,
			&i.ApprovedAt,
			&i.ApprovedByEmployeeID,
			&i.RejectionReason,
			&i.PaidPeriodID,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.EmployeeFirstName,
			&i.EmployeeLastName,
			&i.ApprovedByFirstName,
			&i.ApprovedByLastName,
			&i.TotalCount,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listOvertimeEntriesPaginated = `-- name: ListOvertimeEntriesPaginated :many
WITH filtered AS (
    SELECT
        oe.id,
        oe.employee_id,
        oe.schedule_id,
        oe.entry_date,
        oe.minutes,
        oe.reason,
        oe.description,
        oe.status,
        oe.submitted_at,
        oe.approved_at,
        oe.approved_by_employee_id,
        oe.rejection_reason,
        oe.paid_period_id,
        oe.created_at,
        oe.updated_at,
        ep.first_name AS employee_first_name,
        ep.last_name AS employee_last_name,
        ap.first_name AS approved_by_first_name,
        ap.last_name AS approved_by_last_name,
        COUNT(*) OVER() AS total_count
    FROM overtime_entries oe
    JOIN employee_profile ep ON ep.id = oe.employee_id
    LEFT JOIN employee_profile ap ON ap.id = oe.approved_by_employee_id
    WHERE (
        $3::overtime_status_enum IS NULL
        OR oe.status = $3::overtime_status_enum
    )
    ORDER BY oe.created_at DESC
)
SELECT id, employee_id, schedule_id, entry_date, minutes, reason, description, status, submitted_at, approved_at, approved_by_employee_id, rejection_reason, paid_period_id, created_at, updated_at, employee_first_name, employee_last_name, approved_by_first_name, approved_by_last_name, total_count
FROM filtered
LIMIT $2
OFFSET $1
`

type ListOvertimeEntriesPaginatedParams struct {
	OffsetCount int32               `json:"offset_count"`
	LimitCount  int32               `json:"limit_count"`
	Status      *OvertimeStatusEnum `json:"status"`
}

type ListOvertimeEntriesPaginatedRow struct {
	ID                   uuid.UUID          `json:"id"`
	EmployeeID           uuid.UUID          `json:"employee_id"`
	ScheduleID           *uuid.UUID         `json:"schedule_id"`
	EntryDate            pgtype.Date        `json:"entry_date"`
	Minutes              int32              `json:"minutes"`
	Reason               OvertimeReasonEnum `json:"reason"`
	Description          *string            `json:"description"`
	Status               OvertimeStatusEnum `json:"status"`
	SubmittedAt          pgtype.Timestamptz `json:"submitted_at"`
	ApprovedAt           pgtype.Timestamptz `json:"approved_at"`
	ApprovedByEmployeeID *uuid.UUID         `json:"approved_by_employee_id"`
	RejectionReason      *string            `json:"rejection_reason"`
	PaidPeriodID         *uuid.UUID         `json:"paid_period_id"`
	CreatedAt            pgtype.Timestamptz `json:"created_at"`
	UpdatedAt            pgtype.Timestamptz `json:"updated_at"`
	EmployeeFirstName    string             `json:"employee_first_name"`
	EmployeeLastName     string             `json:"employee_last_name"`
	ApprovedByFirstName  *string            `json:"approved_by_first_name"`
	ApprovedByLastName   *string            `json:"approved_by_last_name"`
	TotalCount           int64              `json:"total_count"`
}

func (q *Queries) ListOvertimeEntriesPaginated(ctx context.Context, arg ListOvertimeEntriesPaginatedParams) ([]ListOvertimeEntriesPaginatedRow, error) {
	rows, err := q.db.Query(ctx, listOvertimeEntriesPaginated, arg.OffsetCount, arg.LimitCount, arg.Status)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListOvertimeEntriesPaginatedRow{}
	for rows.Next() {
		var i ListOvertimeEntriesPaginatedRow
		if err := rows.Scan(
			&i.ID,
			&i.EmployeeID,
			&i.ScheduleID,
			&i.EntryDate,
			&i.Minutes,
			&i.Reason,
			&i.Description,
			&i.Status,
			&i.SubmittedAt,
			&i.ApprovedAt,
			&i.ApprovedByEmployeeID,
			&i.RejectionReason,
			&i.PaidPeriodID,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.EmployeeFirstName,
			&i.EmployeeLastName,
			&i.ApprovedByFirstName,
			&i.ApprovedByLastName,
			&i.TotalCount,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const lockOvertimeEntryByID = `-- name: LockOvertimeEntryByID :one
SELECT id, employee_id, schedule_id, entry_date, minutes, reason, description, status, submitted_at, approved_at, approved_by_employee_id, rejection_reason, paid_period_id, created_at, updated_at
FROM overtime_entries
WHERE id = $1
FOR UPDATE
`

func (q *Queries) LockOvertimeEntryByID(ctx context.Context, id uuid.UUID) (OvertimeEntry, error) {
	row := q.db.QueryRow(ctx, lockOvertimeEntryByID, id)
	var i OvertimeEntry
	err := row.Scan(
		&i.ID,
		&i.EmployeeID,
		&i.ScheduleID,
		&i.EntryDate,
		&i.Minutes,
		&i.Reason,
		&i.Description,
		&i.Status,
		&i.SubmittedAt,
		&i.ApprovedAt,
		&i.ApprovedByEmployeeID,
		&i.RejectionReason,
		&i.PaidPeriodID,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const rejectOvertimeEntry = `-- name: RejectOvertimeEntry :one
WITH updated_overtime_entry AS (
    UPDATE overtime_entries
    SET
        status = 'rejected'::overtime_status_enum,
        rejection_reason = $1::text,
        approved_at = NULL,
        approved_by_employee_id = NULL,
        updated_at = NOW()
    WHERE overtime_entries.id = $2
    RETURNING
        id,
        employee_id,
        schedule_id,
        entry_date,
        minutes,
        reason,
        description,
        status,
        submitted_at,
        approved_at,
        approved_by_employee_id,
        rejection_reason,
        paid_period_id,
        created_at,
        updated_at
)
SELECT
    oe.id,
    oe.employee_id,
    oe.schedule_id,
    oe.entry_date,
    oe.minutes,
    oe.reason,
    oe.description,
    oe.status,
    oe.submitted_at,
    oe.approved_at,
    oe.approved_by_employee_id,
    oe.rejection_reason,
    oe.paid_period_id,
    oe.created_at,
    oe.updated_at,
    ep.first_name AS employee_first_name,
    ep.last_name AS employee_last_name,
    ap.first_name AS approved_by_first_name,
    ap.last_name AS approved_by_last_name
FROM updated_overtime_entry oe
JOIN employee_profile ep ON ep.id = oe.employee_id
LEFT JOIN employee_profile ap ON ap.id = oe.approved_by_employee_id
`

type RejectOvertimeEntryParams struct {
	RejectionReason *string   `json:"rejection_reason"`
	ID              uuid.UUID `json:"id"`
}

type RejectOvertimeEntryRow struct {
	ID                   uuid.UUID          `json:"id"`
	EmployeeID           uuid.UUID          `json:"employee_id"`
	ScheduleID           *uuid.UUID         `json:"schedule_id"`
	EntryDate            pgtype.Date        `json:"entry_date"`
	Minutes              int32              `json:"minutes"`
	Reason               OvertimeReasonEnum `json:"reason"`
	Description          *string            `json:"description"`
	Status               OvertimeStatusEnum `json:"status"`
	SubmittedAt          pgtype.Timestamptz `json:"submitted_at"`
	ApprovedAt           pgtype.Timestamptz `json:"approved_at"`
	ApprovedByEmployeeID *uuid.UUID         `json:"approved_by_employee_id"`
	RejectionReason      *string            `json:"rejection_reason"`
	PaidPeriodID         *uuid.UUID         `json:"paid_period_id"`
	CreatedAt            pgtype.Timestamptz `json:"created_at"`
	UpdatedAt            pgtype.Timestamptz `json:"updated_at"`
	EmployeeFirstName    string             `json:"employee_first_name"`
	EmployeeLastName     string             `json:"employee_last_name"`
	ApprovedByFirstName  *string            `json:"approved_by_first_name"`
	ApprovedByLastName   *string            `json:"approved_by_last_name"`
}

func (q *Queries) RejectOvertimeEntry(ctx context.Context, arg RejectOvertimeEntryParams) (RejectOvertimeEntryRow, error) {
	row := q.db.QueryRow(ctx, rejectOvertimeEntry, arg.RejectionReason, arg.ID)
	var i RejectOvertimeEntryRow
	err := row.Scan(
		&i.ID,
		&i.EmployeeID,
		&i.ScheduleID,
		&i.EntryDate,
		&i.Minutes,
		&i.Reason,
		&i.Description,
		&i.Status,
		&i.SubmittedAt,
		&i.ApprovedAt,
		&i.ApprovedByEmployeeID,
		&i.RejectionReason,
		&i.PaidPeriodID,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.EmployeeFirstName,
		&i.EmployeeLastName,
		&i.ApprovedByFirstName,
		&i.ApprovedByLastName,
	)
	return i, err
}

const updateOvertimeEntryByAdmin = `-- name: UpdateOvertimeEntryByAdmin :one
WITH updated_overtime_entry AS (
    UPDATE overtime_entries
    SET
        schedule_id = COALESCE($1::uuid, schedule_id),
        entry_date = COALESCE($2::date, entry_date),
        minutes = COALESCE($3::integer, minutes),
        reason = COALESCE($4::overtime_reason_enum, reason),
        description = COALESCE($5::text, description),
        updated_at = NOW()
    WHERE overtime_entries.id = $6
    RETURNING
        id,
        employee_id,
        schedule_id,
        entry_date,
        minutes,
        reason,
        description,
        status,
        submitted_at,
        approved_at,
        approved_by_employee_id,
        rejection_reason,
        paid_period_id,
        created_at,
        updated_at
)
SELECT
    oe.id,
    oe.employee_id,
    oe.schedule_id,
    oe.entry_date,
    oe.minutes,
    oe.reason,
    oe.description,
    oe.status,
    oe.submitted_at,
    oe.approved_at,
    oe.approved_by_employee_id,
    oe.rejection_reason,
    oe.paid_period_id,
    oe.created_at,
    oe.updated_at,
    ep.first_name AS employee_first_name,
    ep.last_name AS employee_last_name,
    ap.first_name AS approved_by_first_name,
    ap.last_name AS approved_by_last_name
FROM updated_overtime_entry oe
JOIN employee_profile ep ON ep.id = oe.employee_id
LEFT JOIN employee_profile ap ON ap.id = oe.approved_by_employee_id
`

type UpdateOvertimeEntryByAdminParams struct {
	ScheduleID  *uuid.UUID          `json:"schedule_id"`
	EntryDate   pgtype.Date         `json:"entry_date"`
	Minutes     *int32              `json:"minutes"`
	Reason      *OvertimeReasonEnum `json:"reason"`
	Description *string             `json:"description"`
	ID          uuid.UUID           `json:"id"`
}

type UpdateOvertimeEntryByAdminRow struct {
	ID                   uuid.UUID          `json:"id"`
	EmployeeID           uuid.UUID          `json:"employee_id"`
	ScheduleID           *uuid.UUID         `json:"schedule_id"`
	EntryDate            pgtype.Date        `json:"entry_date"`
	Minutes              int32              `json:"minutes"`
	Reason               OvertimeReasonEnum `json:"reason"`
	Description          *string            `json:"description"`
	Status               OvertimeStatusEnum `json:"status"`
	SubmittedAt          pgtype.Timestamptz `json:"submitted_at"`
	ApprovedAt           pgtype.Timestamptz `json:"approved_at"`
	ApprovedByEmployeeID *uuid.UUID         `json:"approved_by_employee_id"`
	RejectionReason      *string            `json:"rejection_reason"`
	PaidPeriodID         *uuid.UUID         `json:"paid_period_id"`
	CreatedAt            pgtype.Timestamptz `json:"created_at"`
	UpdatedAt            pgtype.Timestamptz `json:"updated_at"`
	EmployeeFirstName    string             `json:"employee_first_name"`
	EmployeeLastName     string             `json:"employee_last_name"`
	ApprovedByFirstName  *string            `json:"approved_by_first_name"`
	ApprovedByLastName   *string            `json:"approved_by_last_name"`
}

func (q *Queries) UpdateOvertimeEntryByAdmin(ctx context.Context, arg UpdateOvertimeEntryByAdminParams) (UpdateOvertimeEntryByAdminRow, error) {
	row := q.db.QueryRow(ctx, updateOvertimeEntryByAdmin,
		arg.ScheduleID,
		arg.EntryDate,
		arg.Minutes,
		arg.Reason,
		arg.Description,
		arg.ID,
	)
	var i UpdateOvertimeEntryByAdminRow
	err := row.Scan(
		&i.ID,
		&i.EmployeeID,
		&i.ScheduleID,
		&i.EntryDate,
		&i.Minutes,
		&i.Reason,
		&i.Description,
		&i.Status,
		&i.SubmittedAt,
		&i.ApprovedAt,
		&i.ApprovedByEmployeeID,
		&i.RejectionReason,
		&i.PaidPeriodID,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.EmployeeFirstName,
		&i.EmployeeLastName,
		&i.ApprovedByFirstName,
		&i.ApprovedByLastName,
	)
	return i, err
}
